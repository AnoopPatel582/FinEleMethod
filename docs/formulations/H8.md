# H8 Element Formulation

FinEleMethod uses an eight-node trilinear hexahedral element for
three-dimensional linear elasticity. ABAQUS `C3D8` elements select this
formulation automatically.

## Assumptions

- Small displacements and small strains.
- Linear, isotropic elasticity.
- Static loading.
- Three translational degrees of freedom per node: (u_x), (u_y), and (u_z).
- Node ordering that produces a positive Jacobian determinant throughout the
  element.

The 24-component element displacement vector is

\[
\mathbf{u}_e=
\begin{bmatrix}
u_1&v_1&w_1&u_2&v_2&w_2&\cdots&u_8&v_8&w_8
\end{bmatrix}^{T}.
\]

## Natural coordinates and node order

The parent element occupies
(-1\leq\xi,\eta,\zeta\leq1). FinEleMethod uses this natural-node order:

| Node | \(\xi_i\) | \(\eta_i\) | \(\zeta_i\) |
| ---: | ---: | ---: | ---: |
| 1 | -1 | -1 | -1 |
| 2 | +1 | -1 | -1 |
| 3 | +1 | +1 | -1 |
| 4 | -1 | +1 | -1 |
| 5 | -1 | -1 | +1 |
| 6 | +1 | -1 | +1 |
| 7 | +1 | +1 | +1 |
| 8 | -1 | +1 | +1 |

Nodes 1–4 form the negative-\(\zeta\) face and nodes 5–8 form the
positive-\(\zeta\) face.

## Trilinear shape functions

Using the natural-coordinate signs from the table, the shape function for node
\(i\) is

\[
N_i(\xi,\eta,\zeta)=
\frac{1}{8}(1+\xi_i\xi)(1+\eta_i\eta)(1+\zeta_i\zeta).
\]

The physical coordinates and displacements are interpolated with the same shape
functions:

\[
\mathbf{x}=\sum_{i=1}^{8}N_i\mathbf{x}_i,
\qquad
\mathbf{u}=\sum_{i=1}^{8}N_i\mathbf{u}_i.
\]

## Jacobian and physical derivatives

The natural derivative matrix is

\[
\mathbf{G}=
\begin{bmatrix}
N_{1,\xi}&\cdots&N_{8,\xi}\\
N_{1,\eta}&\cdots&N_{8,\eta}\\
N_{1,\zeta}&\cdots&N_{8,\zeta}
\end{bmatrix}.
\]

FinEleMethod forms

\[
\mathbf{J}=
\begin{bmatrix}
x_{,\xi}&y_{,\xi}&z_{,\xi}\\
x_{,\eta}&y_{,\eta}&z_{,\eta}\\
x_{,\zeta}&y_{,\zeta}&z_{,\zeta}
\end{bmatrix},
\]

then transforms every derivative using

\[
\begin{bmatrix}
N_{i,x}\\N_{i,y}\\N_{i,z}
\end{bmatrix}
=\mathbf{J}^{-1}
\begin{bmatrix}
N_{i,\xi}\\N_{i,\eta}\\N_{i,\zeta}
\end{bmatrix}.
\]

Non-finite, non-positive, and numerically degenerate determinants are rejected
before stiffness or result calculations proceed.

## Strain-displacement matrix

The project-wide engineering-strain order is

\[
\boldsymbol{\varepsilon}=
\begin{bmatrix}
\varepsilon_x&\varepsilon_y&\varepsilon_z&
\gamma_{xy}&\gamma_{yz}&\gamma_{zx}
\end{bmatrix}^{T}.
\]

The corresponding stress order is

\[
\boldsymbol{\sigma}=
\begin{bmatrix}
\sigma_x&\sigma_y&\sigma_z&
\tau_{xy}&\tau_{yz}&\tau_{zx}
\end{bmatrix}^{T}.
\]

For node \(i\), its (6\times3) contribution to the complete
(6\times24) matrix is

\[
\mathbf{B}_i=
\begin{bmatrix}
N_{i,x}&0&0\\
0&N_{i,y}&0\\
0&0&N_{i,z}\\
N_{i,y}&N_{i,x}&0\\
0&N_{i,z}&N_{i,y}\\
N_{i,z}&0&N_{i,x}
\end{bmatrix}.
\]

Therefore,

\[
\mathbf{B}=
\begin{bmatrix}
\mathbf{B}_1&\mathbf{B}_2&\cdots&\mathbf{B}_8
\end{bmatrix},
\qquad
\boldsymbol{\varepsilon}=\mathbf{B}\mathbf{u}_e.
\]

## Three-dimensional constitutive law

For Young's modulus \(E\) and Poisson's ratio \(\nu\), define the Lamé
parameters

\[
G=\frac{E}{2(1+\nu)},
\qquad
\lambda=\frac{E\nu}{(1+\nu)(1-2\nu)}.
\]

The isotropic elastic law is

\[
\boldsymbol{\sigma}=\mathbf{D}\boldsymbol{\varepsilon},
\]

\[
\mathbf{D}=
\begin{bmatrix}
\lambda+2G&\lambda&\lambda&0&0&0\\
\lambda&\lambda+2G&\lambda&0&0&0\\
\lambda&\lambda&\lambda+2G&0&0&0\\
0&0&0&G&0&0\\
0&0&0&0&G&0\\
0&0&0&0&0&G
\end{bmatrix}.
\]

The shear terms use engineering shear strain, which is why their diagonal
coefficient is (G).

## Element stiffness matrix

The H8 stiffness matrix is

\[
\mathbf{K}_e=
\int_{-1}^{1}\int_{-1}^{1}\int_{-1}^{1}
\mathbf{B}^{T}\mathbf{D}\mathbf{B}\,
\det(\mathbf{J})\,d\xi\,d\eta\,d\zeta.
\]

FinEleMethod uses full (2\times2\times2) Gauss quadrature. Each natural
coordinate is evaluated at \(\pm1/\sqrt{3}\), producing eight integration
points with unit weights:

\[
\mathbf{K}_e=
\sum_{g=1}^{8}
\mathbf{B}_g^{T}\mathbf{D}\mathbf{B}_g\,
\det(\mathbf{J}_g)\,w_g.
\]

The resulting (24\times24) matrix is sent to the same global COO assembly
pipeline used by Q4 analyses.

## Result recovery

At every Gauss point, FinEleMethod calculates

\[
\boldsymbol{\varepsilon}_g=\mathbf{B}_g\mathbf{u}_e,
\qquad
\boldsymbol{\sigma}_g=\mathbf{D}\boldsymbol{\varepsilon}_g.
\]

The six strain and stress components are retained in the project-wide order
defined above. Von Mises stress and the three principal stresses are then
calculated from the recovered three-dimensional stress tensor.

## Implementation map

| Operation | Implementation |
| --- | --- |
| Shape functions and natural derivatives | `src/elements/h8_shape_functions.cpp` |
| Jacobian and inverse | `src/elements/h8_jacobian.cpp` |
| Physical derivatives | `src/elements/h8_physical_derivatives.cpp` |
| Strain-displacement matrix | `src/elements/h8_strain_displacement.cpp` |
| Isotropic constitutive matrix | `src/mechanics/constitutive_matrix.cpp` |
| Gauss integration rule | `src/elements/h8_gauss_quadrature.cpp` |
| Element stiffness | `src/elements/h8_stiffness.cpp` |
| Gauss-point result recovery | `src/elements/h8_recovery.cpp` |

Focused tests cover each operation. The checked-in block-compression benchmark
verifies the complete H8 path against the analytical uniaxial-stress solution.
